<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="PreferenceHeader">
        <!-- Identifier value for the header. -->
        <attr name="id" format="integer"/>
        <!-- The title of the item that is shown to the user. -->
        <attr name="title" />
        <!-- The summary for the item. -->
        <attr name="summary" format="string" />
        <!-- An icon for the item. -->
        <attr name="icon" />
        <!-- The fragment that is displayed when the user selects this item. -->
        <attr name="fragment" format="string" />
    </declare-styleable>

    <declare-styleable name="BaseOnePreferenceFragment">
        <!--The PreferenceHeader file-->
        <attr name="onepref_headerFile" format="reference"/>
    </declare-styleable>

    <declare-styleable name="TwoColumnPreferenceFragment">
        <attr name="onepref_firstColumnWeight" format="float"/>

        <attr name="onepref_secondColumnWeight" format="float"/>
    </declare-styleable>
</resources>